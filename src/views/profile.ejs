<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Perfil de <%= username %></title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <h1>Links de <%= username %></h1>
    <ul>
      <% links.forEach(link => { %>
      <li><a href="<%= link.url %>"><%= link.name %></a></li>
      <% }) %>
    </ul>
  </body>
</html>
